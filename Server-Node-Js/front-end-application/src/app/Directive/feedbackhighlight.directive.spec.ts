import { FeedbackhighlightDirective } from './feedbackhighlight.directive';

describe('FeedbackhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedbackhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
