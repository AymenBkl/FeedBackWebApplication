import { HighlightFeedbackDirective } from './highlight-feedback.directive';

describe('HighlightFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
